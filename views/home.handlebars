<h1>Posts</h1>

{{#each posts}}
    <div>
        <img src="{{image}}" alt="">
        <h2>{{title}}</h2>
        <p>{{body}}</p>
        <a href="/delete/{{id}}">Deletar</a>
        <a href="/edit/{{id}}">Editar</a>
    </div>
{{/each}}